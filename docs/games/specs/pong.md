# Понг (`pong`) — пункт 32

**Info:** `Id="pong"`, `Title="Понг"`, `Accusative="понг"`, `Group=Live`, 2/2, `TickMs=40` (25 Гц),
`Rated=true`, `Hint="Дві ракетки, м'ячик. До семи. Стрілки ↑↓ або W/S, на телефоні — тягни пальцем"`.
`SeatName`: «ліва»/«права».

## Симуляція (на сервері, у дробових одиницях)

- Поле 160 × 100 (ширина × висота), координати `double`. Ракетки: x = 4 (ліва, місце 0) і 156 (права),
  висота 18, ширина 2, швидкість ≤ 60 од/с. М'яч: радіус 1.5, стартова швидкість 60 од/с під випадковим
  кутом ±30° у бік того, хто пропустив (перший раз — випадково), +6% за кожен відбій (макс 160 од/с).
- Відбій від ракетки: кут = (відносне попадання від центру, −1..1) × 60°; від верху/низу — дзеркально.
- Гол: м'яч за x < 0 або x > 160 → очко, пауза 1 с (`serveIn` тиків), новий розіграш.
- Партія до 7. «Готуйсь» 3 с на старті. Тайм-аут раунду — нема.
- Ввід: `Input('move', { dir: -1|0|1 })` (клавіші, утримання) АБО `Input('to', { y: number })` (палець/миша:
  бажана позиція центру ракетки; сервер рухає до неї з обмеженням швидкості). Останній отриманий ввід діє
  до наступного.

## Кадр (кожен тик)

`{ t: number, bx, by, vx, vy, p: [y0, y1], s: [score0, score1], serveIn, winner }` — `t` — номер тика
(для інтерполятора). Округляти до 0.1.

## Вид

`{ scores, phase: 'ready'|'play'|'done', startIn, winner, target: 7 }` (+ останній кадр).

## Клієнт

- Канвас 16:10, малює за інтерполятором `HGames.ui.Interp` (рендер на `requestAnimationFrame`, стан на
  «зараз − 40 мс»), м'яч зі слідом, ракетки кольорами місць. Клавіші: keydown/keyup → `move`. Палець/миша
  на канвасі: `pointermove` → `to` (throttle 30/с — ліміт хаба).
- Рахунок великими цифрами по центру вгорі; «Готуйсь» відлік.

## Тести (≥ 20)

- М'яч летить прямо в ракетку по центру → відбивається горизонтально; в край ракетки → кут ≈ 60°.
- Відбій від верху/низу; швидкість росте на 6%; стеля 160.
- Гол ліворуч → очко правому; пауза; подача в бік того, хто пропустив.
- Ракетка не виходить за поле; `to` рухає не швидше 60 од/с (за 1 тик ≤ 2.4 од).
- До 7 → Finish, Журнал «понг: X 7:4 Y».
- Перф: 5000 тиків < 1 с.
- Детермінізм кутів подачі за сідом.

---

## Як реалізовано

### Файли

| Що | Де |
|---|---|
| Фізика й правила | `src/Hlechyky/Games/Impl/Pong.cs` — `PongCore` (поле, м'яч, ракетки) + `Pong : Game` (кімната, кадри, види, Журнал) |
| Браузер | `web/games/pong.js`, `web/games/pong.css` |
| Тести | `tests/Hlechyky.Tests/Games/PongTests.cs` — 38 тестів (21 на ядро, 17 на кімнату) |

Каркаса не чіпали жодним рядком: гра — звичайний нащадок `Game` з конструктором без параметрів, реєстр
знаходить її сам. Розділ «Потрібно від каркаса» порожній.

### Симуляція

Усе в `PongCore` і в дробових одиницях: поле 160 × 100, вісь y вниз (як у канвасі), крок `Dt = 0.04` с.
Ракетки — центри по y, х фіксовані (4 і 156), швидкість 60 од/с і для клавіш, і для пальця. М'яч — радіус
1.5, подача 60 од/с під випадковим кутом ±30°, кожен відбій від ракетки множить швидкість на 1.06 до стелі
160. Кут відбою = (зсув попадання від центру ракетки, обрізаний до −1..1) × 60°; верх і низ — дзеркало з
поверненням «наскільки заліз, настільки й відскочив».

Три місця, де реалізація точніша за буквальний текст spec:

1. **Відбій рахується по відрізку руху, а не по кінцевій точці.** На 160 од/с м'яч долає за тик 6.4
   одиниці — перевірка «чи він зараз усередині ракетки» пропускала б його наскрізь. `PongCore.Hit` шукає
   перетин площини `край ракетки + радіус` і бере точку попадання саме там (тест
   `A_fast_ball_does_not_tunnel_through_a_paddle`).
2. **Зона попадання — `висота/2 + радіус м'яча`**, а відносне попадання при цьому обрізане до −1..1: край
   ракетки віддає рівно 60°, а м'яч, що зачепив її самим боком, не провалюється у гол. Обидва краї зони
   притиснуті тестами (`A_ball_that_grazes_the_very_corner_of_a_paddle_still_comes_back` і
   `A_ball_just_past_the_corner_of_a_paddle_is_a_goal`).
3. **Точка попадання складається назад у поле (`PongCore.Fold`).** Стіни рахуються вже після ракетки, тож
   пряма «початок тика → кінець тика» може пройти вище стелі чи нижче підлоги, хоча насправді м'яч там
   відскочив. Без складання цілком урятований м'яч ставав голом (перевірка грубого тика проти кроку
   0.2 мс давала 53 такі розбіжності на 200 000 підльотів, зі складанням — жодної). За тик м'яч долає
   щонайбільше 5.6 одиниці по y, тож одного дзеркала досить; тести
   `A_ball_that_bounced_off_the_ceiling_still_reaches_the_paddle` і `…off_the_floor…`.

Гол — центр м'яча за `x < 0` або `x > 160`. Очко, м'яч у центр, `serveIn = 25` тиків (1 с), подача в бік
того, хто пропустив; перша подача — випадковий бік. «Готуйсь» — 75 тиків (3 с), і ракетки в цій фазі вже
слухаються, щоб було де розім'ятись. Партія до 7. Випадковість — тільки `Ctx.Rng`, часу гра не питає взагалі.

### Ввід

`Act` один на обидва наміри (приходять через `Input`, відповіді ніхто не чекає):

- `move { dir: -1|0|1 }` — клавішу тримають; діє, поки не прийде наступний `dir`;
- `to { y }` — палець або миша просять центр ракетки отут; сервер везе її туди зі своєю швидкістю
  (≤ 2.4 од за тик), тож миша не дає переваги над клавіатурою.

Кожен новий ввід скасовує попередній (`to` гасить утримання і навпаки). Обидва приймаються і як
`{ dir: 1 }`, і як голе число. Невідома дія — `ActResult.Fail("Тут так не ходять")`.

### Кадр і вид

Кадр летить щотика, повні види — лише коли змінилось те, чого в кадрі не видно (кінець «готуйсь», очко,
кінець партії).

```jsonc
// Frame()
{ "t": 78, "bx": 80.0, "by": 50.0, "vx": 59.1, "vy": -10.4,
  "p": [50.0, 62.4], "s": [3, 4], "serveIn": 0, "startIn": 0, "winner": null }
```

**Відхилення від spec:** у кадрі є ще `startIn`. Spec його там не називав, але відлік «Готуйсь 3…2…1»
міняється щотика, а види в цій фазі вже не летять — і платформа прямо каже брати реалтайм-статус із
`ctx.frame`, а не з `ctx.view` (INTEGRATION-NOTES §3). Одне мале число на кадр дешевше, ніж повний вид
75 разів поспіль.

```jsonc
// View(seat) — однаковий для обох місць і для глядача, ховати тут нема чого
{ "scores": [3, 4], "phase": "play", "startIn": 0, "winner": null, "target": 7,
  "turn": null, "frame": { … } }
```

`winner` — номер місця (`0` ліва, `1` права) або `null`; нічиїх у понзі не буває. `phase: "done"` накриває
**обидва** кінці партії: і сьоме очко, і техпоразку — `Pong.OnLeave` віддає перемогу тому, хто лишився за
столом, і аж тоді кличе базовий. Інакше після «Встати» суперника канвас застиг би у фазі «граємо» без
жодного підсумку. `turn: null` — ходів тут
нема, але каркас питає це поле в кожної гри. Останній кадр вкладено у `frame`: так картка малює поле ще до
першого тика (стіл, що чекає на суперника, виглядає як поле, а не як порожнеча).

Рядок Журналу — у стилі решти платформи, з назвами місць: `Понг: Оля ліва 7:3 Петро права` (spec писав
коротше, «понг: X 7:4 Y», але сусідні ігри пишуть саме так, і чіпи місць у картці підписані тими ж словами).

### Клієнт

- Канвас 320 × 200 (16:10), одиниця поля = 2 пікселі. Малюється не «кожен кадр як прийшов», а через
  `HGames.ui.Interp` на `requestAnimationFrame`: стан беремо на «зараз − один інтервал» і змішуємо два
  останні кадри. На голі (рахунок у сусідніх кадрах різний) інтерполяцію вимикаємо — вести м'яч через усе
  поле назад у центр було б брехнею.
- М'яч тягне слід із 10 позицій (лише поки летить), ракетки — кольорами місць (`--accent` ліва,
  `--ok` права), рахунок великими цифрами по центру вгорі, «Готуйсь» і «Виграв X» — затемненням поверх поля.
- Клавіші: `onKey` ловить `keydown`, свій слухач на документі — `keyup` (каркас `keyup` не роздає). Шлемо
  **лише зміну** напрямку, а не кожне натискання: автоповтор клавіші інакше з'їв би квоту хаба. Пішли з
  вкладки із затиснутою клавішею — `blur` спиняє ракетку. Затиснута клавіша головніша за мишу (`to` на
  сервері гасить утримання, тож випадковий рух курсором над полем не має вбивати керування), а нова партія
  («Ще раз») починається з повторної відправки напрямку — сервер про клавішу, яку не відпускали, не знає.
- Палець і миша: `pointermove`/`pointerdown` на канвасі кладуть бажану позицію в стан, а відправляє її
  rAF-цикл не частіше ніж раз на 40 мс (25/с, із запасом до хабової стелі в 30). Остання позиція не
  губиться — вона просто чекає наступного вікна. Цикл живий, поки картка в DOM, але в невидиму картку
  (пішли на «Ефір» — вкладка ховається через `display: none`) не малює: інакше телефон гріє акумулятор
  заради застиглої картинки.
- `.pongboard.play { touch-action: none }` вішається лише тому, хто грає: глядач гортає сторінку пальцем як
  завжди.
- Хрестовина каркаса (`ui.dpad`) тут не годиться — вона на клік, а ракетку треба **тримати**. Тому свої дві
  кнопки `↑`/`↓` на `pointerdown`/`pointerup`, видимі лише при `pointer: coarse`. Вказівник кнопка
  захоплює сама (`setPointerCapture`): `pointerleave` не спливає, а без захоплення «натиснув мишею на
  сенсорному ноуті, з'їхав із кнопки, відпустив» лишало б ракетку їхати в стіну.

### Обмеження, які лишились свідомо

- **Пропущені тики не надолужуються** — так влаштований `TickEngine` (заснув сервер — м'яч не телепортується).
- **Лаг гравця сервер не компенсує**: правда одна, вона на сервері, клієнт лише малює її з відставанням на
  один інтервал. Для понга на локальній мережі цього досить.
- **Ракетка не «підкручує» м'яч власним рухом** — кут задає лише точка попадання, як і сказано в spec.
- Коротка партія (< 20 с) черепків не приносить: у реалтайму `Moves == 0`, і економіка дивиться лише на час
  (INTEGRATION-NOTES §5). Це не баг понга.

### Як перевірити руками

1. `appsettings.Local.json` із `Site:ListenPort: 8109` і відведеним убік ефіром (Liquidsoap 11234,
   Icecast 18000, AutoDj/DjBot/Deploy вимкнені), `New-Item -ItemType Directory -Force logs`,
   `dotnet build src/Hlechyky/Hlechyky.csproj -nodeReuse:false`, далі `Start-Process dotnet` із
   `-PassThru`; зупиняти строго за своїм PID.
2. Дві вкладки: `http://localhost:8109` і `http://127.0.0.1:8109` (різні `localStorage` → різні ніки).
   «Ігри» → «Швидкі» → «+ Стіл» на «Понг» → у другій вкладці «Сісти».
3. Що має бути видно: три секунди «Готуйсь» з цифрою на полі; стрілки ↑↓ (і W/S) возять ракетку, поки
   клавішу тримають; миша над канвасом тягне ракетку за курсором; рахунок росте після кожного гола, і м'яч
   секунду стоїть у центрі; на сьомому очку — «Виграв X», рядок у Журналі, кнопка «Ще раз».
4. «Ще раз» обертає місця й обнуляє рахунок; «Встати» посеред партії — техпоразка й рядок «…встав з-за
   столу»; F5 посеред партії лишає місце за собою (grace 20 с).
5. Вузьке вікно (375 px): картка не ламає розкладку, під полем з'являються дві кнопки-утримання, скрол
   сторінки пальцем по полю в гравця вимкнений, у глядача — ні.

---

## Оновлення 24.09.2026 — арена на трьох-чотирьох

**Чому.** За весь час на проді понг зіграли один раз. Причин бачилось три: стіл тільки на двох (а друзі
збираються компанією, і для двох уже є улюблені Мотоцикли), партія без жодного «смаку» (кут вирішувала лише
точка попадання, нічого не клацало й не спалахувало) і дрібне, розтягнуте канвасом поле. Тож понг став
грою для компанії, а класика на двох — живішою.

**Що змінилось.**

- `Info`: 2–4 гравці, `Start = ByHost` (господар тисне «Почати» — і на двох теж, бо за стіл може сісти
  третій), `Rated = false`. Ело й ставки платформа знає лише для ігор рівно на двох (`Rooms.ReadStake`,
  `Rewards.Elo`); понг за всю історію зіграли раз, тож рейтингу втрачати було нічого. Опція `len`
  («Партія»): `short` — до 5 / 3 життя, `normal` — до 7 / 5 життів (типово), `long` — до 11 / 7 життів.
- **На двох — класика** (`PongCore`), як була, плюс:
  - **підкрутка** (`SpinAngle = 15°`): ракетка, що в мить удару їде, додає до кута 15° у свій бік (стеля
    ті самі 60°). Рух рахується за фактичним зсувом ракетки за тик; тремтіння пальця (менше пів кроку)
    не враховується;
  - двоє можуть сидіти не на місцях 0 і 1 (третій сів і встав у лобі): ліва — менше місце, права —
    більше; кадр несе `seats: [ліве, праве]`, ввід мапиться на ракетку за місцем;
  - у кадрі ще `hit` (хто відбив у цьому тику) і `rally` (удари поспіль без гола) — для спалахів і звуку.
- **На трьох-чотирьох — арена** (`PongArena`): квадрат 120 × 120, стіни 0 ліва, 1 права, 2 верхня,
  3 нижня (місце = стіна), глухі квадрати 14 × 14 у кутах, ракетка 22 завдовжки ходить уздовж своєї
  стіни між кутами. У кожного `Lives` життів; м'яч за твоєю стіною — мінус життя, а тому, хто торкався
  м'яча останнім, — «влучання» (`Goals`). Життя скінчились — стіна глухне (м'яч від неї відскакує); порожнє
  місце — глуха стіна від першої секунди. Останній живий виграв; Журнал:
  `Понг, арена: Оля (2 ♥) · Ігор · Петро` (далі — хто вилетів пізніше, той вище).
  - Фізика — два півкроки на тик (кути й до чотирьох ракеток), відбій від ракетки — по відрізку руху, як і в
    класиці. Швидкість 55 → ×1.05 за відбій → стеля 140, подача з центру ±25° у бік того, хто щойно
    пропустив (або випадкового живого).
  - Проти вічних відскоків між двома глухими стінами: після відбою від глухого м'яч іде вздовж стіни
    щонайменше з 25% швидкості (`MinSlide`), а якщо 10 с ніхто його не торкнувся — подача наново без
    штрафу (`IdleTicks`).
  - Встав посеред арени — твоя стіна глухне, решта грає далі (рядок у Журналі); лишився живим один — його
    перемога.
  - Кадр: `{ mode: 'arena', t, bx, by, vx, vy, p[4], l[4], serveIn, startIn, winner, hit, rally, lost, from, n }`
    (`p`/`l` — null для порожніх місць; `n` — номер подачі, щоб клієнт не тягнув м'яч через поле після гола).
    Вид: `{ mode, lives, goals, phase, startIn, winner, target, turn, frame }`. Вид класики має `mode: 'duo'`.
  - Імена місць на арені — кольори ракеток («жовта», «зелена», «руда», «синя»): кожен бачить свою стіну
    внизу, тож «ліва»/«верхня» кожному значили б своє.
- **Стіл у лобі показує те поле, на якому гратимуть**: двоє — класика, троє — арена. Дограний стіл, який
  відкрив новий гравець, показує свіже поле, а не старий підсумок.
- **Клієнт** (`pong.js`):
  - арена повернута так, що **своя стіна завжди внизу**: керування в усіх однакове — ← → / A D, палець
    ліворуч-праворуч, на телефоні кнопки ← →; клієнт сам перераховує напрямок і позицію пальця в
    координату своєї стіни; глядач бачить арену як є;
  - канвас у справжньому розмірі (560 × 350 класика, 480 × 480 арена) — на Full HD не милиться; арена
    обмежена висотою вікна, на телефоні — на всю ширину;
  - «соки»: спалах ракетки на ударі, м'яч «розжарюється» зі швидкістю, трус поля й червоний спалах стіни на
    гол, спливні «+1» / «−1 ♥» / «Оля ➜ Петро», лічильник серії від 6 ударів, «це ти» біля своєї
    ракетки під час відліку, підсумок «👑 нік — остання ракетка на арені»; `prefers-reduced-motion` гасить трус;
  - звук (WebAudio, лише після жесту людини): клацання удару, стіни, гол, виліт, фанфари; вимикач під полем
    (`localStorage['pong.sound']`);
  - легенда під ареною: фігура місця (● ■ ▲ ◆ — не лише колір), нік, життя, влучання;
  - джойстик: `pad.dirs = true` (на арені потрібні ← →).
- **Тести**: `PongArenaTests.cs` — 30 нових (арена: режими, відбої, кути, глухі стіни, «ковзання», простій,
  виліт, перемога на трьох і чотирьох, вихід посеред партії, рематч, ввід верхньої ракетки, детермінізм, перф;
  класика: старт господарем, підкрутка, коротка партія, невідома довжина, двоє на місцях 0 і 2, відкритий
  наново стіл). У `PongTests.cs` оновлено три (старт господарем, паспорт 2–4, лічильник видів).
