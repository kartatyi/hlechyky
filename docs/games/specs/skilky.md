# Скільки? (`skilky`) — пункт 44

**Info:** `Id="skilky"`, `Title="Скільки?"`, `Accusative="«Скільки?»"`, `Group=Party`, 2/12,
`Start=ByHost`, `Hidden=true` (відповіді приховані до розкриття), `TickMs=1000`, `Rated=false`,
`Hint="Питання, на яке ніхто не знає точної відповіді. Кожен пише число: що ближче — то більше очок, найближчому ще й бонус. П'ять питань"`.

> **15.09.2026 — новий баланс і пам'ять.** Очки 3/2/1 за місце замінено очками за точність, бонусом найближчому
> й бонусом за швидкість; запитання не повторюються, доки є свіжі (див. «Баланс» і «Пам'ять» нижче). Там, де
> правила нижче кажуть «+3/+2/+1» чи «≥ 150», діє вже нове.

## Правила

- Партія = 5 запитань. Банк: `data/questions/skilky.json` — **≥ 150** запитань `{ q, a, unit?, src? }`
  українською: суміш загальних (висота Говерли в метрах, довжина Дніпра, кількість клавіш у фортепіано,
  рік заснування Львова…) — з правильними відповідями, які агент **перевіряє** (без вигаданих чисел; сумнівні
  — не брати) — і **динамічних** із бази радіо (`{ q: "Скільки треків зіграло на радіо за останні 7 днів?", dyn: "plays7d" }`,
  `plays30d`, `likesTotal`, `tracksTotal`, `voiceTotal`, `chatTotal`, `minutesPlayed30d`, `topRequesterCount7d`) —
  значення обчислює `SkilkyStats` (WP G-skilky) через `Db` (додати **тільки** методи читання в окремому
  файлі `Games/Impl/SkilkyStats.cs` з власними SQL-запитами через `Db` — Db.cs не чіпати; якщо `Db` не дає
  доступу до з'єднання, `SkilkyStats` відкриває своє через шлях `Paths.Resolve("data/hlechyky.db")`).
- Раунд: показ запитання, 30 с на відповідь (`answer {value}`, число; можна змінювати до кінця часу; видно
  лише «відповів/ні»). Після 30 с (або коли всі відповіли) — розкриття: правильна відповідь, усі відповіді
  відсортовані за відстанню; найближчий +3, другий +2, третій +1 (при рівній відстані — обом однаково). Пауза
  6 с, наступне.
- Хто не відповів — 0. Після 5 запитань `Finish([лідери])`, переможцям `Award(seat, 5, "award:skilky")`
  (через каркасну виплату: `Winners` — і так WinReward; тому `Award` НЕ кличемо, каркас платить сам).
- Дядько Глек: перед кожним розкриттям — `Ctx.Say(...)` з коротким статичним коментарем (банк із 15 фраз:
  «Найближче — Оля, промахнулась усього на 12»). `DjBrain.FlavorAsync` — не використовуємо (не варто $).

## Дії

`answer {value: number}` — під час фази `ask`.

## Вид (Hidden)

```ts
{ round, of: 5, phase: 'ask'|'reveal'|'between'|'done', question: string, unit: string|null,
  endsAt: string (ISO), answered: boolean[],
  my: number|null,
  reveal: null | { answer: number, years: boolean, rows: [{ seat, value, diff, points, accuracy, bonus, fast }] },
  scores: number[],
  result: null | { winners: number[], scores: number[] } }
```
`points = accuracy + bonus + fast`; `years` — запитання про рік (міряється в роках, а не у відсотках).
У фазі `ask` чужі значення відсутні (тест).

## Клієнт

- Велике питання, поле для числа з кнопкою «Відповісти» (Enter), дуга-таймер `timerArc`, список гравців
  з галочками «відповів». Розкриття: правильна відповідь великими, таблиця відстаней із підсвіткою
  переможців раунду. Рахунок партії праворуч.

## Тести (≥ 20)

- Банк: ≥ 150, усі мають `q`, число `a` або `dyn`; жодних дублікатів `q`; динамічні обчислюються на порожній базі (0).
- Раунд: відповідь приймається/оновлюється до дедлайну, після — Fail; всі відповіли → розкриття раніше.
- Очки 3/2/1, рівні відстані; хто не відповів — 0; 5 раундів → Finish, лідери (нічия можлива).
- Hidden: чужі відповіді відсутні в `ask`; присутні в `reveal`.
- Вибір запитань без повторів у партії; детермінізм за сідом.

---

# Як реалізовано

## Баланс (15 вересня 2026, друга редакція): точність, першість і швидкість

**Чому.** При 3/2/1 за місце гра вдвох не мала сенсу: той, хто промазав у десять разів, однаково брав +2,
кожне питання важило одне очко різниці. Перша редакція (5/3/2/1 і +2 найближчому) виправила це, але
вдарила по малих числах: правильна 9, один пише 9 — 7 очок, другий пише 8 (це 11 %) — 2. Друга редакція —
рішення користувача того ж дня: м'якша шкала, промах на одиницю — «майже», найближчий завжди має хоч
одне очко, а рівність розбиває швидкість.

**Шкала** (`Skilky.Accuracy`, кожне число оцінюється саме, без огляду на суперників):

| Звичайні числа: промах від відповіді | Роки (`unit == "рік"`): різниця | Очки |
|---|---|---|
| до 2 % | точно | 5 (`Skilky.Bullseye`) |
| до 10 % | ±2 | 4 |
| до 25 % | ±5 | 3 |
| до 50 % | ±15 | 2 |
| до двох разів (тобто до +100 %) | ±50 | 1 |
| далі, а також 0 і від'ємні | далі | 0 |

- **Промах на одиницю** на цілій відповіді дає щонайменше 4 (`Skilky.OffByOne`): 8 на 9, 3 на 2. На дробових
  відповідях (1,852 милі, 2,54 см) правило не діє — там одиниця це пів відповіді.
- **Найближчому +1** (`Skilky.BestBonus`) — завжди, навіть коли за точність він нічого не взяв: тоді це його
  єдине очко («очко втіхи»). Рівна відстань (з допуском `SameDiff`) — бонус усім однаково близьким.
- **Швидкість** (`Skilky.SpeedBonus`, `SpeedGap` = 1 с): у кожній групі однакової відстані (не лише серед
  найближчих) той, хто надіслав своє число раніше за всіх інших у групі хоча б на секунду, бере ще +1. Час —
  останньої зміни числа: хто передумав, той відповів пізніше. Менше за секунду — ніхто не швидший.
- Роки міряються роками, бо відсотки там брешуть: 1900 проти 1990 — «лише 4,5 %».
- Межі включні з допуском 1e-9: 2,794 проти 2,54 — рівно 10 % → 4, хоч double каже 0.10000000000000009.
- Максимум за питання — 7 (5 + 1 + 1). Кінець партії, нічия, виплати — без змін.
- Приклади: відповідь 9 → 9 дає 5 (+1 найближчому = 6), 8 — 4, 7 — 3, 5 — 2, 15 — 1; відповідь 100 → 90 — 4,
  80 — 3, 60 і 150 — 2, 180 — 1, 250 — 0; рік 1991 → 1990 — 4, 1995 — 3, 2001 — 2, 2030 — 1.
- Вид: у рядках розкриття `points = accuracy + bonus + fast`. Клієнт пише «+6» і під ним «5+1», у найближчого 🏆,
  у швидшого ⚡, у підказці — «5 за точність, 1 найближчому». Шкала словами — `RULES` у `skilky.js` (міняти разом
  із сервером), показується в лобі й у паузі перед запитанням.
- Дядько Глек: на точне число — окремі фрази («Точнісінько — Петро!»), коли найближчий нічого не набрав за
  точність — «ніхто навіть близько… одне очко — за сміливість», інакше — старі 15 фраз.

## Соло, налаштування столу й черепки за очки (15 вересня 2026)

- **Соло.** `MinPlayers = 1`: господар може натиснути «Почати» сам, а хто встигне підсісти до старту — грає
  разом. Стіл стоїть у «Компанії», інші бачать і можуть дивитись. Самому бонус «найближчому» не дається
  (найближчий з одного — не заслуга), решта шкали та сама. Якщо з двох один встає, другий догравує сам;
  партія закінчується лише коли за столом нікого (`Finish([])`). Самотній стіл у лобі каркас прибирає
  за звичним правилом «засиджений» (`Rooms.Housekeeping` рахує мінімум двох), а відповідь на створення
  стола каже «Можна почати самому або дочекатись друзів».
- **Налаштування при «+ Стіл»** (`GameOption`, каркас сам малює випадайки й відкидає чужі значення):
  «Питань» 3/5/7/10/15 (типово 5), «Час на відповідь» 15/30/45/60 с (типово 30), «Теми»: усі, Україна,
  музика й кіно, наука й природа, світ і спорт, IT і техніка — **можна кілька** (див. нижче). Тема — поле `topic` у банку
  (`SkilkyTopics`): нові запитання отримали тему свого пакета, старі 182 розмічено вручну; динамічні — тема
  `radio`, вони йдуть лише в «Усі теми». У кожній темі від 177 до 308 запитань. Вид несе `seconds` — для дуги.
- **Кілька тем** (15 вересня 2026, прохання користувача «не одну чи всі»). Опція `topic` — `GameOption` з
  `Multi: true`: у попапі теми стоять чипами, «Усі теми» гасить решту, будь-яка тема гасить «Усі теми»,
  вимкнули останню — «Усі теми» вмикаються самі. На дроті — рядок через кому (`"ukraine,science"`);
  `Rooms.Effective` лишає знайомі теми в порядку паспорта, «all» серед обраних перемагає, порожнє → «all».
  Гра читає це `SkilkyTopics.Parse` → множина тем (null — усі, з радіо). Шапка столу підписує вибір
  «Україна · Наука, природа й тіло». Старий клієнт з однією темою працює як і раніше.
- **Черепки за очки.** Наприкінці партії кожен, хто сидить за столом, отримує `очки / 5` черепків
  (`Skilky.PointsPerShard`, округлення вниз) — і в компанії, і самому. **Без денної стелі** — рішення
  господаря сайту. Гра кличе `Ctx.Award(seat, n, "points:<номер партії>")`, `Rewards` платить через
  `Economy.Grant` з ref `points:<гра>:<кімната>:<партія>:<нік>` (повтор події не платить удруге, «Ще раз» —
  платить), у гаманці це «очки за партію — Скільки?». Звичайна виплата каркаса за перемогу/нічию/участь
  (5/2/1, лише в компанії, 10 партій на гру на день) лишається як була — вона йде окремо.
- Ризик, про який варто пам'ятати: без стелі соло з довідником під рукою може дати до ~7 черепків за хвилину.
  Якщо почнуть фармити — стеля ставиться одним рядком у `Rewards` (`GrantCapped`).

## Пам'ять: запитання не повторюються без потреби (15 вересня 2026)

- `SkilkySeen` (своя таблиця `skilky_seen(nick_key, q_key, seen_at, times)` через `Db.With`, як у реклами)
  пам'ятає, хто яке запитання бачив і коли. Ключ ніка — `EconomyStore.Key` (без регістру й пробілів по краях),
  ключ запитання — `SkilkyQuestion.Key`, 16 hex SHA-256 від тексту: переписане запитання вважається новим.
- «Бачив» — з тієї секунди, коли запитання з'явилось на екрані (перехід `between → ask`), для всіх, хто сидить
  за столом. Запитання, до яких недограна партія не дійшла, лишаються свіжими.
- Вибір (`Skilky.Pick`): увесь пул тасується сідом кімнати, далі `SkilkySeen.Freshest` стабільно сортує за
  «коли хтось із цього столу бачив востаннє»: спершу не бачені ніким, далі бачені найдавніше. Отже, ті самі
  друзі не побачать повтору, доки в банку є свіжі запитання, а новачок за столом нічого не «з'їдає» в інших:
  рахується найсвіжіший раз серед усіх присутніх.
- Без бази (тести з порожнім провайдером) або при будь-якій помилці SQLite пам'ять мовчить — запитання просто
  тасуються, як раніше. Для гравців без історії вибір рівно такий самий, як без пам'яті (тест).

## Файли

| Файл | Що там |
|---|---|
| `src/Hlechyky/Games/Impl/Skilky.cs` | правила: фази, час, очки, кінець партії, види й кадри, фрази Глека |
| `src/Hlechyky/Games/Impl/SkilkyBank.cs` | читання банку запитань (кеш на процес, ніколи не кидає) |
| `src/Hlechyky/Games/Impl/SkilkyStats.cs` | динамічні відповіді зі статистики радіо (свій SQL через `Db.With`) |
| `src/Hlechyky/Games/Impl/SkilkySeen.cs` | пам'ять «хто яке запитання бачив» і вибір найсвіжіших |
| `data/questions/skilky.json` | банк: **1130** записів — 1122 статичні й 8 динамічних |
| `web/games/skilky.js`, `web/games/skilky.css` | модуль у браузері |
| `tests/Hlechyky.Tests/Games/SkilkyTests.cs` | 38 тестів |
| `.gitignore` | виняток `!data/questions/` — банк їде в гіті разом із грою |

## Банк запитань

- **15.09.2026: 1130 записів** (1122 статичні й 8 динамічних; було 182). Нові 948 — одинадцять тематичних пакетів,
  кожен написав окремий агент за спільними правилами (перевірні числа, стабільні приблизні з «приблизно» в тексті й
  розкидом ≤ ±25 % у `src`, без населення/цін/рекордів, що б'ються, без трагедій і політики), а потім **незалежний
  перевіряльник** звірив кожне число з джерелами й прибрав двозначності. Теми й скільки лишилось після перевірки:
  Україна — географія 89 і історія/культура 89; музика 90; кіно, серіали й ігри 89; тіло й їжа 82; природа 76;
  космос і наука 90; світова географія й споруди 89; спорт, побут і одиниці 88; техніка й історія 82; IT 85.
  Перевіряльники знайшли й виправили кілька справжніх пасток (віадук Мійо: 343 м — з пілоном; вік Всесвіту без
  «млрд» у тексті; «32 м'язи у вусі кішки» — міф; 161 стаття Конституції — лише первісна редакція). Дублі між
  пакетами прибрано окремим проходом по всьому банку. `src` у гру не потрапляє — це записка для людей.
- Роки — приблизно чверть банку (288 з 1130).
- **Одиниці з множником** («млн км», «млрд років», «тис. км²»): людина може написати повне число. `Skilky.InUnits`
  бере те прочитання (як є чи поділене на множник), яке ближче до правди за порядком величин: 13 800 000 000 на
  «млрд років» — це 13,8, а 1200 на «тис. км²» лишається 1200.
- Дублікатів `q` і ключів немає (тести). Формат кореня — об'єкт `{ note, questions: [...] }`; парсер
  приймає і голий масив. Криві записи (без тексту або без жодної відповіді) мовчки відкидаються, бо одна
  помилка в JSON не має гасити гру.
- Числа — тільки такі, що перевіряються довідником і не змінюються від місяця до місяця. Усе спірне
  («скільки супутників у Юпітера», «скільки об'єктів ЮНЕСКО в Україні», населення міст) навмисно
  викинуто ще на етапі складання: гра про відчуття величини, а не про свіжість цифри.
- Динамічні (`dyn`) рахує `SkilkyStats` через `Db.With(...)` — гачок, який `Db` дає економіці; сам `Db.cs`
  не змінювався. Ключі: `plays7d`, `plays30d`, `likesTotal`, `tracksTotal`, `voiceTotal`, `chatTotal`,
  `minutesPlayed30d`, `topRequesterCount7d`. Голосові (`tracks.id LIKE 'voice-%'`) рахуються окремо й у
  «різні треки» не потрапляють; системні рядки Журналу не рахуються за балачки.
- **Динамічне запитання потрапляє в партію, лише коли його відповідь > 0.** Питати «скільки треків зіграло
  за тиждень», коли відповідь нуль, — не загадка. На порожній базі (і взагалі без `Db`, як у тестах із
  порожнім провайдером) грають самі лише статичні запитання; `SkilkyStats` там чесно віддає 0.

## Відхилення від spec (свідомі, усі дрібні)

1. **Фаза `between` — це коротка пауза (3 с) ПЕРЕД кожним запитанням**, а не після. Spec перелічує чотири
   фази, але описує цикл із трьох; я віддав `between` роль «зараз буде питання»: інакше запитання падає
   людині на голову посеред набирання числа з минулого раунду. Порядок: `between` 3 с → `ask` 30 с →
   `reveal` 6 с → наступне `between` (або `done`). У фазі `between` поле `question` порожнє — щоб зазирнути
   в консоль на три секунди раніше за інших не вийшло.
2. **«Усі відповіли → розкриття раніше» настає найближчим тиком, тобто до секунди пізніше.** Усі переходи
   фаз живуть у `Tick()`; `Act` лише ставить дедлайн на «зараз». Так у грі одне місце, де міняється фаза, і
   `Act` не мусить сам розсилати види (для реалтайм-кімнати `Rooms.Act` їх і не шле).
3. **Партія, де ніхто не набрав жодного очка, завершується нічиєю** (`Finish([])`), а не «перемогою всіх
   дванадцятьох». Якщо очки в когось є — лідери (їх може бути кілька) отримують перемогу, як і просить spec.
4. **`Award(seat, 5, "award:skilky")` не кличеться** — саме так, як spec і каже в дужках: за партію платить
   каркас із `RoomFinishedEvent`.
5. **Фрази Глека переписані так, щоб нік у них завжди стояв у називному** (підметом або одразу після тире).
   Приклад зі spec («Найближче — Оля, промахнулась усього на 12») має дієслово в жіночому роді — на ніку
   «Петро» це виглядає кепсько, а відмінювати чужі імена ми не вміємо. Тому «Найближче — Петро: різниця 12».
   Фраз 15, плюс три на випадок, коли не відповів ніхто.
6. **Вид не має поля `turn`** — його в spec і нема, а каркас без нього просто не малює «Твій хід» (тут ходів
   і немає, є спільний таймер). Рядок статусу пише сам модуль.
7. **Число приймається і рядком**: клієнт шле те, що людина набрала («10 000», «2,54»), сервер прибирає
   пробіли (зокрема нерозривні) і читає кому як крапку. `{value: 2061}`, голе число і голий рядок — усе
   годиться. Назад число теж іде українським: пробіли між тисячами й кома в дробовій частині («1 000 000,25»).
8. **«Рахунок партії праворуч» — лише там, де картка достатньо широка.** Столи стоять у сітці
   `repeat(auto-fill, minmax(300px, 1fr))`, тож на екрані 1600 px картка все одно виходить ~400 px: бічна
   колонка з'їла б половину запитання. Тому картка міряє себе сама (`container-type: inline-size`), і
   рахунок від'їжджає в колонку 150 px праворуч від `@container (min-width: 520px)`, а на звичайній вузькій
   картці лягає під таблицю. Перевірено обидва стани (674 px — праворуч, 380 px — під таблицею).

## Виправлено за рецензією

- **Рівна відстань на дробових числах давала різні очки.** Яруси будувались через `Distinct()`/`IndexOf`
  по `double`, тобто побітово: |36.4 − 36.6| і |36.8 − 36.6| математично однакові, а в бітах — ні, і на
  екрані виходило «різниця 0,2 → +3» проти «різниця 0,2 → +2». Тепер яруси рахуються проходом по вже
  відсортованих рядках із відносним допуском (`SameDiff`, 1e-9). Тест шукає сід із дробовою відповіддю й
  промах, який у `double` виходить двома різними числами.
- **Після «Ще раз» рахунок секунду показував підсумок минулої партії** (та ще й під новою розсадкою):
  `paintScores` брала `scores` з `ctx.frame` без перевірки, а кадр після рематчу ще старий. З'явилась
  спільна перевірка свіжості `fresh(ctx, v)` — нею тепер користуються і `ticks()`, і `paintScores()`.
  `status()` бере фазу з виду: сервер міняє фазу тільки разом із видом, тож кадр її не випереджає.
- **Дробові числа показувались із крапкою**, хоча людина набирала кому й поруч стояло «1 000 000». Тепер і
  сервер (`Num`), і клієнт (`num`) віддають українське число: пробіли між тисячами, кома в дробовій
  частині. Кому на сервері ставимо руками, а не культурою `uk-UA` — збірка може піти в
  `InvariantGlobalization`, і культура мовчки віддала б крапку.
- **Вісім з'єднань до SQLite на кожен «Почати»/«Ще раз».** `SkilkyStats` тримає порахуані числа
  `SkilkyStats.Ttl` (5 хв) у кеші свого примірника (тобто своєї кімнати — між кімнатами й тестами нічого
  не тече), а обіцянка «ніколи не кидає» стала правдою: ловиться будь-який виняток, а не два конкретні
  (`Convert.ToInt64` міг дати `FormatException`, `Open()` — `IOException`, і партія падала б цілком).
- **Рахунок партії праворуч** — зроблено там, де картка це витримує (див. відхилення №8).
- Прибрано мертву змінну `--skgold` із `:root` у `skilky.css`: підсвітку робить `--skrow`.

## Обмеження

- Стіл на 12 місць: у шапці картки каркас малює всі місця, тож на телефоні над полем висить рядок із
  дев'яти «вільно». Це поведінка `core.js` (спільний файл), а не гри — див. «Потрібно від каркаса».
- Сісти посеред партії не можна (правило каркаса: `Join` пускає лише в `Lobby`). Хто спізнився — дивиться
  збоку до кінця партії, далі «Ще раз» уже з ним.
- Гра не `Persistent`: рестарт сервера партію не переживає (як і всі столи).
- Рейтинг Ело не ведеться (`Rated: false`), ставок теж нема — каркас дозволяє їх лише для `MaxPlayers == 2`.

## Потрібно від каркаса

- Нічого обов'язкового. **Побажання:** `core.js` (`headHtml`) малює чіпи всіх місць, зокрема вільних. Для
  ігор на 2–4 це добре, для столу на 12 — пів картки на телефоні. Було б корисно згортати вільні місця в
  один чіп («+9 вільно») там, де `maxPlayers > 4`.

## Тести

38 тестів у `tests/Hlechyky.Tests/Games/SkilkyTests.cs` (вимога spec — ≥ 20):

- **Банк** (5): ≥ 150 запитань; у кожного є текст і рівно одна з відповідей (`a` XOR `dyn`); жодного
  дубліката `q`; усі ключі `dyn` відомі `SkilkyStats`; відсутній і битий файл дають порожній банк, а не виняток.
- **Статистика** (5): на порожній базі всі ключі — 0; без `Db` — теж 0; на живій базі рахуються програші,
  лайки, треки, голосові, балачки, хвилини й найактивніший замовник; старі програші випадають із вікна тижня;
  порахуване число живе `SkilkyStats.Ttl` (5 хв) і лише потім знову йде в базу.
- **Кімната й фази** (5): `ByHost` чекає на господаря, у паузі запитання ще нікому не видно; відповідь
  приймається й міняється до дедлайну; «10 000» і «2,54» — теж числа; нечисло/чужа дія — `Fail` і стан не
  змінився; до запитання й після дедлайну — `Fail`; усі відповіли → розкриття без чекання.
- **Очки** (4): 3/2/1 і нуль четвертому; рівна відстань — рівні очки, а наступна відстань стає другим
  ярусом; рівна відстань до дробової цілі (де в double вона виходить двома різними числами) — теж рівні
  очки; хто мовчав, у таблиці розкриття відсутній і має 0.
- **Кінець** (6): п'ять запитань і перемога найточнішого (з `scores` у `RoomFinishedEvent`); рівна партія —
  двоє переможців; партія без жодного числа — нічия; Глек говорить перед кожним із п'яти розкриттів;
  «Ще раз» дає чистий стан і обертає місця; вихід одного з трьох партію не спиняє, а вихід передостаннього — спиняє.
- **Приховане, види, кадри** (5): чужого числа немає ні у виді сусіда, ні у виді глядача, ні в кадрі;
  після розкриття всі числа на столі; форма виду — зі spec; кадр компактний і без секретів; тик без
  новин шле лише кадр, а чиясь відповідь — ще й види.
- **Вибір запитань** (4): без повторів у партії; той самий сід — ті самі запитання, інший сід — інші;
  без бази динамічні не питаються, з живою базою — трапляються.
- **Каталог і перф** (2): гра в каталозі як `party`/`byHost`/`hidden`, `tickMs 1000`, є `skilky.css`;
  1000 тиків столу на 12 гравців із відповідями — менш ніж за 2 с (`[Trait("Category","Perf")]`).

Уся збірка: `Passed! - Failed: 0, Passed: 305` (було 267 до цієї гілки).
`dotnet build src/Hlechyky/Hlechyky.csproj -c Release -nodeReuse:false` — 0 попереджень, 0 помилок.

## Як перевірити руками

1. `appsettings.Local.json` у worktree (порт 8116, ефір відведено вбік), далі
   `New-Item -ItemType Directory -Force logs`, `dotnet build`, `Start-Process dotnet -ArgumentList
   'src/Hlechyky/bin/Debug/net10.0/Hlechyky.dll' -PassThru -RedirectStandardOutput logs/dev.out.log`.
   У лозі має бути `Now listening on: http://[::]:8116` і `liquidsoap not reachable at start-up`.
2. Дві вкладки (`http://localhost:8116` і `http://127.0.0.1:8116` — різні `localStorage`, різні ніки),
   вкладка «Ігри» → «Компанія» → «+ Стіл» → «Поставити стіл». Другий тисне «Сісти», господар — «Почати».
3. Через три секунди з'являється запитання й дуга на 30 с. Пишеш число, Enter — під полем «Твоє число: …»,
   у списку гравців з'являється галочка. Коли відповіли всі — розкриття настає одразу.
4. У розкритті: правильна відповідь великим, під нею всі числа за відстанню з +3/+2/+1, найближчий
   підсвічений. Рахунок партії — праворуч, якщо картка ширша за 520 px, інакше під таблицею.
   Через шість секунд наступне запитання.
5. Після п'ятого — рядок Журналу «Скільки?: Петро 12, Ганна 8, Іван 4», тост гаманця й ачівки, кнопка
   «Ще раз» (місця обертаються, рахунок обнуляється).
6. «Встати» посеред партії третім гравцем — партія триває далі без нього. Коли лишається один — партія
   закривається його перемогою.
7. F5 посеред партії — місце вціліло (grace 20 с), картка перемалювалась із поточним запитанням і рахунком.
8. Третя вкладка (або підписка без місця) бачить `seat: null`, `my: null` і жодного чужого числа до розкриття.
9. Вузьке вікно (380 px): горизонтального скролу нема, поле й кнопка стають на всю ширину, стовпчик
   «різниця» ховається.

### Що перевірено 9 вересня 2026 (порт 8116)

Перевірено все з пунктів 1–9. Партія на трьох (Петро в браузері, Ганна й Іван — окремими з'єднаннями до
того самого хаба з тієї ж вкладки, щоб не займати чужі вкладки в спільній панелі браузера):

- ✅ каталог віддає `skilky` як `party`/`byHost`/`hidden`, `/games/skilky.js` і `.css` — 200;
- ✅ «+ Стіл» → «Почати» → через 3 с запитання «Скільки хвилин у тижні?», дуга є;
- ✅ у полі набрано `1 000` і натиснуто Enter → «Твоє число: 1 000», галочка «✓ Петро» у всіх;
- ✅ щойно відповіли всі троє — розкриття тієї ж секунди: «Правильна відповідь 10 080 хвилин»,
  рядки «Петро 1 000 / різниця 9 080 / +3», «Ганна 900 / 9 180 / +2», «Іван 5 / 10 075 / +1»;
- ✅ у жодному з чотирьох видів фази `ask`, що прилетіли сусідові по столу, чужого числа немає (перевірено
  пошуком по сирому JSON події `room`); глядач бачить `seat: null`, `my: null` і лише галочки;
- ✅ Журнал: «Петро, Ганна і Іван сіли грати в «Скільки?»» і в кінці «Скільки?: Петро 12, Ганна 8, Іван 4»;
- ✅ Дядько Глек сказав своє перед кожним розкриттям, щоразу іншою фразою;
- ✅ ачівки й черепки прилетіли всім трьом (це вже каркас);
- ✅ «Ще раз» — місця обернулись (Ганна 1, Іван 2, Петро 3), рахунок 0/0/0, «Питання 1 з 5»;
- ✅ Іван «Встав» посеред партії — стіл грає далі вдвох;
- ✅ F5 посеред партії — місце Петра вціліло, картка ожила з поточним запитанням і рахунком;
- ✅ 380 px: `scrollWidth == clientWidth` (380), картка 330 px, поле на всю ширину.

### Що перевірено після виправлень (9 вересня, порт 8116)

Свій сервер на своєму порту, дві вкладки (`127.0.0.1:8116` — Петро, `localhost:8116` — Ганна), партія до кінця:

- ✅ жодної помилки в консолі в обох вкладках;
- ✅ «Записав: 1 789,5» і «Твоє число: 1 789,5» — кома там, де людина її набрала;
- ✅ у розкритті «Ганна 1 000 000,25 / різниця 999 998,25 / +2» — пробіли між тисячами й кома;
- ✅ «Ще раз» одразу дає «Ганна 0, Петро 0» (шість замірів по 150 мс поспіль), хоча минула партія
  скінчилась на 11:7 — старий кадр більше не малюється;
- ✅ картка 674 px: рахунок стоїть праворуч колонкою 150 px із лінією-роздільником, той самий рядок;
- ✅ вікно 380 px: `scrollWidth == clientWidth` (380), картка 330 px, поле на всю ширину, рахунок під таблицею.

**Чого перевірити не вдалось:** як саме цокає цифра всередині дуги. Панель браузера тримає документ у стані
`visibilityState: 'hidden'`, а `timerArc` із `core.js` крутиться на `requestAnimationFrame`, який у схованій
вкладці не викликається — цифра лишається на «0». Перевірено натомість, що елемент `.garc` створюється,
`endsAt` парситься браузером (`Date.parse` повертає валідний час, попри сім знаків у частках секунди) і що
до дедлайну лишається рівно стільки, скільки має: у фазі `ask` — 30 с. У живому браузері цокати має.

**Дрібниця, яку виправлено після першого прогону:** підпис «Питання 1 з 5» стояв під `text-transform:
uppercase` і читався як «ПИТАННЯ 1 3 5» — велика «З» і трійка в системному шрифті близнюки. Uppercase знято.

## Оновлення 24.09.2026 (нічний прохід, гілка `sweep/party2`)

Гра популярна (46 партій на проді), тому правила й баланс не чіпали — лише те, що видно наприкінці й на екрані.

- **Підсумок партії «Як це було».** Після останнього запитання під розкриттям — список усіх запитань партії:
  правильна відповідь і хто підібрався найближче (кілька однаково близьких — усі), з їхнім числом і очками.
  Сервер збирає це в `Reveal` (`Recap`), у виді — поле `recap` лише у фазі `done` (посеред партії `null`, щоб не
  відволікало). Клієнт малює `<details open>`, згорнуте людиною не розгортає назад на кожному кадрі.
- **Розкриття ожило.** Правильна відповідь «падає» (`skpop`), рядки випливають по черзі від найближчого (`--n` на
  рядку, `skrise`); `prefers-reduced-motion` вимикає обидва.
- **Курсор сам у полі**, щойно запитання з'явилось (лише на ПК: на телефоні клавіатура закрила б запитання; і
  лише коли людина не друкує деінде — у Балачках, наприклад).
- **Відмінки одиниць.** Банк пише одиниці формою «багато» («років», «хвилин»), і на екрані було «72 років». Клієнт
  (`unitFor`) для ~40 найчастіших лічильних одиниць ставить «рік/роки», «хвилина/хвилини»…; дробові числа й
  одиниці з «млн»/«тис.» лишаються як є.
- **«у 1 804,8 раза більше»** → від десяти разів ціле: «у 1 805 разів».
- **Баг: Дядько Глек обіцяв «очко втіхи» самотньому гравцеві**, хоча соло бонусу «найближчому» не дають. Тепер
  для соло без очок за точність — свій пул `Lonely` («Мимо: Оля повз на 2 784. Цього разу без очок.»). Тест.
- Кількість місць — без змін (1–12): уже покриває будь-яку компанію.
- Тести: +3 (`The_finished_match_shows_every_question_with_the_truth_and_the_closest`,
  `Equally_close_players_share_the_line_in_the_recap`, `Alone_and_way_off_uncle_Hlek_does_not_promise_a_consolation_point`,
  а перший ще й перевіряє, що посеред партії `recap == null`).
- Перевірено в headless Chrome: 3 гравці (ПК 1280), соло на телефоні 375 (без горизонтального скролу), Full HD 1920.
